# Shared PostgreSQL connection settings for all Spring Boot services
# (Do not commit real secrets; use your environment/.env injection)
DB_HOST=localhost
DB_PORT=5000
DB_NAME=myapp
DB_USER=appuser
DB_PASSWORD=dbuser123
